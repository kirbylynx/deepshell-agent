语言：简体中文 | [English](v0.1.5.md)

# DeepShell Agent v0.1.5 单文件运行时版本

> **产品：** DeepShell Agent
>
> **标语：** A desktop agent powered by DeepSeek Harness.
>
> **版本：** `v0.1.5`
>
> **版本性质：** 运行时架构版本：已安装的 Node.js + DSH 依赖树被按平台的 Enhanced SEA（单可执行文件）sidecar 取代，安装不再携带数万个运行时文件，同时 Session、Provider、凭据与官方 DSH Web UI 保持不变。
>
> **状态：** **已准备为未签名 prerelease 候选。** Windows x64 W0–W3 验收已在最终 clean rebuild 产物上通过（installed/portable 矩阵、升级与回退路径、on-device SEA 证据、用户真机工具复测）；macOS MAC-01 至 MAC-09 已在 macOS 轮次通过。CI run `36252177821`（success）仅作为同 commit 的独立构建门禁记录。Developer ID/公证与 Windows 代码签名**尚未**完成。
>
> **日期：** 2026-09-26

## 1. 版本范围

- **SEA 单文件运行时**（`REL-028`）：每个安装包只携带一个按平台的 `deepshell-runtime.exe`（Enhanced SEA、Zstd、`@yao-pkg/pkg@6.22.0`），取代解压式的 `runtime/node` + `runtime/dsh` 树；终端用户不再安装或落盘 Node.js、npm、pnpm 或 DSH，机器上也无需预装它们；
- **受控运行时 Cache**：native addon 物化在 `%APPDATA%\com.deepshell.agent\runtime-cache\native\win32-x64\<SEA 摘要前 16 位>`；Cache 按记录摘要逐文件校验与修复、在崩溃遗留后重建，并保持 generation 锁；
- **打包门禁**：NSIS 安装器、portable ZIP、三个 schema 6 package manifest、schema 3 package report、按平台 source-input digest，以及带下述两项声明例外的体积门禁；
- **Windows self-spawn 分流**（`W2-F007`/`W2-F008`，见第 8 节）：SEA entry 现在同时分流两条自 spawn 路径——pkg 的 `child_process` 标记（`PKG_EXECPATH`）与 DSH subprocess Job runner / `windows-acl` sandbox runner 使用的 Windows 原生 `CreateProcess`——并让脚本参数稳定位于 `process.argv.slice(2)`。

## 2. 实测数据

### 2.1 Windows x64（最终 Windows-local 资产）

| 项目 | v0.1.4 基线 | v0.1.5 | 变化 |
| --- | ---: | ---: | ---: |
| 安装树 | 340,319,800 B / 27,426 文件 | **187,022,831 B / 33 文件** | **-153,296,969 B / -27,393 文件** |
| portable staging / extracted | 340,008,126 B / 27,427 文件 | 186,916,511 B / 34 文件 | **-153,091,615 B / -45.0%** |
| NSIS 安装器 | 56,777,588 B | **86,524,831 B** | **+52.4%**（声明例外） |
| portable ZIP | 111,700,110 B | **103,250,079 B** | **-7.6%** |
| 首次运行 Runtime-owned 文件 | v0.1.4 未测量 | **1,745** 总计（安装树 33 + Runtime-owned 外置 1,712） | 门禁：不超过 2,000 |

### 2.2 启动与内存（Windows x64，Standard 对照 SEA）

| 门禁 | 要求 | v0.1.5 实测 | 结论 |
| --- | --- | ---: | --- |
| Warm Ready Gate | +20% / +2 s | 5,903.2 ms vs 6,539.9 ms（-9.7%，-636.8 ms） | 通过 |
| First-cache Ready Gate | +50% / +5 s | 6,818.3 ms vs 6,539.9 ms（+4.3%，+278.4 ms） | 通过 |
| Warm RSS | +20% / +64 MiB | 296,224 KiB vs 194,864 KiB（+52.0%，+101,360 KiB） | **声明例外** |

### 2.3 macOS arm64（macOS 侧交接证据）

| 项目 | v0.1.4 基线 | v0.1.5 | 变化 |
| --- | ---: | ---: | ---: |
| macOS `.app` | 438,102,894 B / 30,220 文件 | **204,876,047 B / 33 文件** | **-233,226,847 B / -30,187 文件** |
| macOS DMG | 247,455,775 B | **107,815,210 B** | **-139,640,565 B** |

上表 macOS 数据与 MAC-01 至 MAC-09 结果来自 macOS 侧交接记录（`docs/plans/v0.1.5-sea-runtime/macos-handoff.md`）；macOS 侧将在 combined release staging 时基于最终 `v0.1.5` 提交重建其资产。

## 3. Windows 验收摘要

| 项目 | 结果 |
| --- | --- |
| portable 全新安装与首次启动 | 通过（`runtime_ready`） |
| v0.1.4 installed → v0.1.5 升级 | 通过（NSIS hook 后无 legacy `runtime/node` + `runtime/dsh` 残留） |
| v0.1.4 portable → v0.1.5 portable 切换 | 通过（共享用户数据） |
| installed ↔ portable 交替使用 | 通过（连续三次 ready，无残留进程） |
| Session/Profile 保留 | 通过（前后各 8 个会话） |
| Release 无 Standard Runtime | 通过（解压树中无 `node`/`dsh` 路径） |
| Native Cache 损坏与并发 | 通过（逐文件摘要修复、generation 锁） |
| 正常退出 / 强杀 / 孤儿清理 / 带孤儿卸载 | 通过 |
| 卸载 ownership（dead/alive/foreign/forged） | 通过 |
| 在 SEA proxy 文件存在时回退 v0.1.4 | 通过 |
| Windows Defender 扫描 | 通过（SEA 0.5 s / NSIS 0.2 s / ZIP 2.3 s，无威胁） |
| 真机工具复测（用户，portable 构建） | 通过：`read` / `write` / `edit` / `glob` / `grep` / `pwsh` 全部可用 |

## 4. 资产

Windows-local 资产（本轮，manifest 非 provisional）：

- `DeepShell Agent_0.1.5_x64-setup.exe` — 86,524,831 B / SHA256 `55b80187d82e8e5150295fa6dba9fdf2a02287f000d1b49dbc9ab05ff45a0768`；
- `DeepShell.Agent_0.1.5_x64-portable.zip` — 103,250,079 B / SHA256 `6de7d83017246fefd5b2ea9502ecbec4e5339bf4acf53886b0674f4ed2342338`；
- SEA 运行时：166,663,680 B / SHA256 `a0f12d514e949ad52fcd383a8dabf0d5d758a7a0d6a1d50c4e575cb4a2c573a9`；
- `windowsSourceInputSha256` = `binarySourceInputSha256` = `ae23c46e248ba71ff17d6649f89f9ec1c63f9ad54ed9ec08555c5aa892b3a6dc`；
- 资产来源提交：`3e65099`（其后的文档类提交不改变构建输入）。

`SHA256SUMS.txt`、license inventory、SBOM、schema 3 package report、security audit、on-device 与性能证据，以及 Windows-local 溯源记录都在本地 release staging 生成并随 Windows 回传包交付。Windows 交接记录（[`docs/plans/v0.1.5-sea-runtime/windows-handoff.md`](plans/v0.1.5-sea-runtime/windows-handoff.md)）承载本地溯源、四态汇总与 macOS C2 的下一步。CI run `36252177821`（success）仅作为构建门禁记录，其 artifact 不作为发布资产。本版本**未**完成 Windows 代码签名与 macOS Developer ID 签名/公证。

## 5. 已声明门槛例外（用户已确认并在此披露）

`runtime/manifest/windows-gate-exceptions.json` 中声明了两项例外。声明显式且可审计；验收工具只放行已声明项，schema 3 report 在 `gateExceptions` 数组中记录它们；未声明的超标仍然阻断。

| 例外 | 要求 | 实测 | 归因 |
| --- | --- | ---: | --- |
| `warmRss` | +20% / +64 MiB | 296,224 KiB vs 194,864 KiB（+52.0%，+101,360 KiB） | Windows 平台 Node SEA 会把 raw asset archive（约 59.5 MB）作为私有内存常驻；macOS 上同数据为 file-backed 且可回收（macOS 实测约 +8.3%）。这是应用层无法消除的平台实现差异。 |
| `windowsNsis` | +10% | 86,524,831 B（+52.4%） | 内嵌 Zstd archive 无法再压缩，而 v0.1.4 的 NSIS 对 `node_modules` 文本具有极高 LZMA 压缩率。portable ZIP 仍低于基线（-7.6%）。 |

## 6. 已知限制（如实记录）

- 预览版未签名：全新机器上预计出现 Windows SmartScreen 信誉警告与 macOS Gatekeeper 提示。验收机器上 SmartScreen 未报警（该机可能已放宽），仍需在干净机器上人工确认；
- CI artifact 只是构建门禁，不属于发布资产；
- 本版本未执行：系统关机/注销路径、GUI 卸载提示交互、清理超时注入、数据回退，以及 `REL-025` 的 macOS 范围（隔离记录退休修复仍仅限 Windows）；
- `dsh web --open` 的浏览器接管路径在 SEA 运行时下不可用（它以 `node --input-type=module --eval` 形态自 spawn）；桌面应用固定以 `--no-open` 启动，因此该路径在应用内不可达；
- 父进程被异常强杀时可能遗留孤儿 SEA sidecar；下一次受控启动或卸载时由 ownership registry 与卸载 hook 终止（真机已验证）。

## 7. 验证

- `pnpm check` 全绿：Rust 54、unit 83 通过（4 跳过）、contract 9、integration 1、security 1；
- `pnpm runtime:sea:verify`（含新增的 `self-spawn-shapes` 与 Windows `sandbox-acl` 链门禁）、`runtime:sea:compare`（25,436 文件）、`runtime:sea:smoke`、`pnpm runtime:verify`、`pnpm profile:verify`、`pnpm package:compare`、`pnpm release:windows:check` 全部通过；
- `runtime:sea:on-device`、installed-tree 捕获、size gate、schema 3 package report、benchmark 与 E2E/Release 安全边界比较均在最终资产上通过；
- 第 3 节的真机工具复测由用户在本机 portable 构建上完成。

## 8. 验收后 review 修复（Windows SEA 阶段）

Windows 阶段执行了 review-fix-loop，并在本次交接前关闭了以下 finding（全部在最终资产上复验）：

- `W2-F001`（Blocker）：64 字符 Cache generation 目录在真实 app-data 下超出 `MAX_PATH`，导致 sharp/koffi 无法加载；generation 目录名改为 SEA 摘要前 16 位；
- `W2-F002`（Blocker，已确认例外）：warm RSS 超出门槛；已声明并披露；
- `W2-F003`（Blocker）：v0.1.4 → v0.1.5 升级遗留 legacy `runtime/node` + `runtime/dsh` 树；NSIS `POSTINSTALL` hook 现在精确清理这两个目录；
- `W2-F004`（Blocker）：真机上所有 spawn 子进程的工具失败，因为 Windows Job runner 会重入 SEA，packaging bootstrap 必须消费 `DSH_SUBPROCESS_RUNNER`；entry 加入互斥分流链（self-spawn 脚本、runner selector、探针、CLI）；
- `W2-F005`（Blocker）：`glob`/`grep` 失败，因为 DSH 搜索 helper 需要 `<executable>-rg.exe`，而 native helper 无法来自 VFS；ripgrep sidecar 随 SEA 一起 staging 并校验；
- `W2-F006`（Medium）：宿主模式 package report 未默认读取本机 Windows manifest，首个运行文件数缺失；
- `W2-F007`（Blocker）：`pwsh` 工具报 `error: --profile <name> is required`——`windows-acl` 沙箱会把命令包装成 runner argv，而 DSH 的 Windows Job runner 通过原生 `CreateProcess` 拉起，从不携带 `PKG_EXECPATH`；entry 现在同样识别 `argv[2]` 的快照内绝对脚本；
- `W2-F008`（High）：自 spawn 的快照脚本丢首个参数，因为 argv 归一化同时移除了 entrypoint 占位与脚本路径；现在只移除占位，脚本参数稳定在 `argv[2]`；
- `W3-F001`（Medium，审计一致性）：门槛例外声明曾硬编码确认轮的实测值；该文件属于构建输入，任何重建都会让记录值漂移却仍被读成最终值。现在 rationale 只保留归因，实测权威值指向 schema 3 report 的 `runtimeAcceptance.gates` 与 size gate 的 `windowsNsis.bytes`，并在本文档披露。修复提交 `3e65099`，随后从 clean commit 重建（input digest `58f4d1ae…` → `ae23c46e…`；SEA exe 字节不变：`a0f12d51…`）。

修复由常驻探针覆盖：`self-spawn-shapes` 断言两条自 spawn 路径的参数契约，`subprocess-chain` 覆盖 `cmd`、`powershell`、`pwsh` 与 `sandbox-acl` 链（Job runner → 原生 `CreateProcess` → ACL runner → 受限令牌子进程）。
