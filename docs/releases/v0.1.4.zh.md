语言：简体中文 | [English](v0.1.4.md)

# DeepShell Agent v0.1.4 打包效率版本

> **产品：** DeepShell Agent
>
> **标语：** A desktop agent powered by DeepSeek Harness.
>
> **版本：** `v0.1.4`
>
> **版本性质：** 打包效率版本：按平台裁剪运行时、新增 Windows 免安装分发、平台原生菜单，以及有界的运行时清理加固。
>
> **状态：** **已作为未签名 GitHub prerelease 发布。** Windows WIN-01 至 WIN-13 已在真实 Windows 11 x64 的最终重建产物上通过，macOS MAC-01 至 MAC-07 已在最终 macOS arm64 构建上通过；发布使用权威 Windows-local NSIS/portable 资产与本轮重建 DMG。Developer ID/公证与 Windows 代码签名尚未完成。
>
> **日期：** 2026-09-20

## 1. 版本范围

- **按平台裁剪运行时**（`REL-022`）：每个安装包只携带本平台实际执行的 Node 运行时；
- **Windows 免安装 ZIP**（`REL-023`）：可解压即用的第二种 Windows 分发形式；
- **平台原生菜单**（`DESK-023`）：macOS 保留应用菜单，Windows 使用 `File > Exit` 与 `Help > About DeepShell Agent`；
- **退出与卸载清理**（`REL-024`）：Windows 侧现在如实报告进程树终止失败；NSIS 卸载器在删除文件前运行受限维护命令，清理属于当前安装的残留；
- **包体积回归门禁**（`QA-011` 的包体积子范围）：tracked 基线、按平台构建输入 digest 与三方 manifest 体积门禁。

## 2. 实测数据

### 2.1 macOS arm64

| 项目 | v0.1.3 基线 | v0.1.4 | 变化 |
| --- | ---: | ---: | ---: |
| macOS `.app` | 545,566,355 B / 32,214 文件 | **438,102,894 B / 30,220 文件** | **-107,463,461 B / -1,994 文件** |
| macOS DMG | 289,373,346 B | **247,455,775 B** | **-41,917,571 B** |

`.app` 只包含 `runtime/node/darwin-arm64`，Node、DSH 与 Profile 子树均与已验证源树等价。`macosSourceInputSha256` 为 `e522712c0b5fb0bfba4da1087ac3ccb416d678194d3234243c5682f46536fb7b`。

### 2.2 Windows x64（真实设备）

| 项目 | v0.1.3 基线 | v0.1.4 | 变化 |
| --- | ---: | ---: | ---: |
| Windows 安装树 | 536,901,173 B / 32,226 文件 | **340,319,800 B / 27,426 文件** | **-196,581,373 B / -4,800 文件** |
| NSIS 安装包 | 84,350,550 B | **56,777,588 B** | **-27,572,962 B** |
| portable staging / extracted | 首版 | 340,008,126 B / 27,427 文件 | 建立首版基线 |
| portable ZIP | 首版 | 111,700,110 B | 建立首版基线 |

安装树不再包含 `runtime/node/darwin-arm64`；随包的 Node、DSH 与 Profile 子树与已验证源树逐字节等价。

`windowsSourceInputSha256` 为 `06bfdfafe29a1b9afbe611bbb321a8bcddb8dc4e5814add63bab1906c5e14a64`。

## 3. 验收摘要（Windows x64）

| ID | 验收项 | 结论 |
| --- | --- | --- |
| WIN-01 | Runtime 结构（NSIS 与 ZIP 只含 `win32-x64`） | ✅ 实测通过 |
| WIN-02 | NSIS 构建 / 安装 / 首启 / 退出 / 卸载 | ✅ 构建 / 安装 / 首启 / 退出 / 卸载已实测；真实会话创建已在最终构建上验证 |
| WIN-03 | ZIP 解压启动与退出 | ✅ 解压 / 启动 / 退出已实测；真实会话创建已在最终构建上验证 |
| WIN-04 | 安装版与免安装版共用数据目录 | ✅ 数据共享与跨形态 UI 级会话/Provider 双向读取均已验证 |
| WIN-05 | 跨形态单实例 | ✅ 实测通过 |
| WIN-06 | WebView2 前置与说明 | ✅ |
| WIN-07 | 正常退出无 Sidecar | ✅（窗口关闭与菜单退出矩阵） |
| WIN-08 | 存在遗留 Sidecar 时卸载 | ✅ 安装树完整删除 |
| WIN-09 | 删除免安装目录保留用户数据 | ✅ |
| WIN-10 | 体积门禁 | ✅（安装树、NSIS 与 portable 均已记录） |
| WIN-11 | 原生菜单 | ✅ |
| WIN-12 | 免安装启动无安装痕迹 | ✅ 启动前后快照零差异 |
| WIN-13 | 前向升级边界 | ✅ v0.1.3 数据目录可读写且无迁移错误；代表性 v0.1.3 旧会话已在最终构建上列出、打开并继续发送消息；README 已警告不支持降级 |

## 4. 发布产物

- `DeepShell.Agent_0.1.4_aarch64.dmg` — SHA256 `4f4169013478e62aac1c67bc72d8f2605022d088924ffe0f9362f9d7a47607ef`
- `DeepShell.Agent_0.1.4_x64-setup.exe` — SHA256 `4038eda58868196dc14d0d2b4aa8940397ea8954ac6d6391420621e967b0b8da`
- `DeepShell.Agent_0.1.4_x64-portable.zip` — SHA256 `6725f6ea5d3a28a9675b059c9f3a590e8b4c09422262aeb0efc102165851c512`

`SHA256SUMS.txt`、license inventory、SBOM、combined package report、security audit 与 Windows-local provenance 记录均在本地 release staging 中生成。provenance 记录把 Windows 资产绑定到源码提交 `03fc6bea9aa509e9df63688efc8aa03182cb527f`、digest `06bfdfaf…`、回传 ZIP hash 与两项二进制 hash。CI run `35488211340` 仅作为构建门禁登记，其 artifact 未进入 release staging。本版本**未**完成 Windows 代码签名与 macOS Developer ID 签名 / 公证。

## 5. 已知限制（如实记录）

- `REL-025`：隔离记录退休修复仅落在 Windows 侧；macOS 进程树仍带该缺陷；
- `REL-026`：文件拖拽仍被 Tauri 层吞掉；
- `REL-027`：客户端渲染进程死亡仍未被检测与恢复；
- 最终审计中 production root、bundled DSH Runtime 与 Rust 均为零通告；开发/测试工具链仍有 4 个 high 与 1 个 moderate 告警，按非阻塞构建工具告警记录，不据此宣称全部依赖零漏洞；
- 系统关机/注销路径未运行；应用菜单 Quit 与窗口关闭两条路径已实测并安全清理受管 Sidecar。

## 6. 验证记录

- `pnpm check` 在 Windows 全绿：Rust 49、Unit 65、Contract 9、Integration 1（真实 DSH Runtime）、Security 1；
- `pnpm runtime:verify`、`pnpm profile:verify`、`pnpm package:compare`、`pnpm release:windows:check` 全绿；
- 退出矩阵、跨形态单实例、免安装无痕迹启动、免安装删除与卸载矩阵均记录在本地验收证据中；会话创建、跨形态 UI 级会话/Provider 读取、免安装目录删除与代表性旧会话升级已在最终重建产物上补充执行并通过；
- macOS 最终 `pnpm check` 全绿：Rust 54、Unit 68（另 1 skipped）、Contract 9、Integration 1、Security 1；`runtime:smoke`、`package:verified`、ad-hoc `codesign --verify --deep --strict`、`hdiutil verify` 均通过；
- macOS 最终 UI 目视确认 About/Services/Hide/Hide Others/Quit、About 版本与 runtime 信息，并确认 Quit 与窗口关闭均安全清理 Sidecar；
- combined package report、三资产 `--require-assets` staging 与最终 `SHA256SUMS.txt` 全部通过。

## 7. 发布后复核修复

发布前对 Windows 阶段全部改动做了六轮复核（含 macOS C0 修复触发的 W7 全量返验与 W7b 验收口径修复），修复了一批正确性与可重复性问题；上述测量数据均在最终构建（`31a3442` 叠加验收状态更新，win32-x64 源码输入指纹 `06bfdfaf…`）上重新采集。

- **进程身份失败关闭加固**：卸载清理不再忽略身份校验失败、不再处理非 active 的所有权记录，PID 复用场景绝不会被终止（有专门回归测试注入该场景）；进程存活与路径校验区分"无法确认"与"已退出"，未知状态拒绝继续；所有外部 PowerShell/taskkill 调用均带超时。
- **可重复性**：新增 `package:windows:installed-tree` 命令编排"安装 → 捕获 → 卸载 → 环境恢复"，并把注册表/快捷方式恢复排在卸载完全结束之后（修复了恢复被卸载器后续步骤删除的竞态）。
- **边界与契约**：CI workflow 上传 Windows 平台 manifest（nsis 与 portable）；release staging 支持 `--require-assets` 供 combined staging 校验；卸载 hook 在静默安装下不弹窗、并在清理后等待文件句柄释放；About 元数据从 runtime-lock 读取 Node/DSH 版本而不再硬编码；包清单不再在 `resources` 重复整棵安装树。
- **第二轮复核**：installed-tree 编排在无法确认卸载完成时失败关闭（Abort 场景不再删除半安装目录、也不再把旧注册表覆盖回去伪装成"环境已恢复"），并拒绝 `runtime/staging/install-tree-v<version>` 之外的任意 `--install-root`；启动恢复在无法校验进程身份（例如 PowerShell 被策略禁用）时隔离记录并继续启动，退出与卸载路径仍失败关闭；`taskkill` 等待受调用方 deadline 约束，tar/cargo/strings 辅助调用显式带超时。重跑的 Windows 冒烟矩阵（干净状态、应用运行中、窗口关闭退出、强杀孤儿清理、卸载与注册表恢复）在最终构建上通过，同 commit 的 CI run `35218713142` 全绿。
- **第三轮复核**：NSIS hook 在 `ExecWait` 前清零并预置失败码（`ClearErrors` / `StrCpy $R0 15` / `IfErrors`），维护二进制无法启动时绝不会被误判为"清洁"；size gate、package report、release staging 与 runtime helper 的大文件哈希改为流式；package report 增加 portable ZIP 与磁盘归档的一致性比对；redaction 覆盖仓库根路径；installed-tree 卸载 settle 窗放宽到 90 秒（超时后补一次确认），portable tar 超时放宽到 30 分钟，杀软环境下的合法长耗时操作不再被误杀。重跑的真机冒烟矩阵在最终构建上全部通过（干净状态 0（两次）/ 应用运行中 13 / 窗口关闭退出无残留 / 孤儿清理 0 / 卸载并恢复注册表），同 commit 的 CI run `35245353581` 全绿。
- **第四轮复核**：roadmap、architecture 与 release 文档现已与登记的 Windows x64 部分验收对齐（含 CI 实际上传的两个 Windows manifest，并保留 WIN-02/03/04/13 的会话、UI 与代表性升级未运行项）；审计日志的 Node/DSH 版本改为从 runtime-lock 注入（带一致性测试），token 脱敏覆盖 `--token=` 片段，根路径脱敏大小写不敏感，sidecar stderr 诊断文件落盘前先做 token 脱敏；诊断包只读取大日志的尾部窗口，baseline 捕获改为流式哈希。重跑的真机冒烟矩阵在最终构建上全部通过（干净状态 0（两次）/ 应用运行中 13 / 窗口关闭退出 / 孤儿清理 0 / 卸载并恢复注册表），同 commit 的 CI run `35298236895` 全绿。
- **W7 全量返验（由 macOS C0 修复触发）**：Windows 完整重跑发现并关闭两个跨平台缺陷——C0 的菜单 `cfg` 修复在 Windows 留下 `ItemKind::Separator` 死代码（`clippy -D warnings` 编译失败）、报告测试在 Windows 主机断言了 macOS 专属条件；均已在 `31a3442` 修复。完整 CLI 矩阵（T1–T11，含 PID 复用隔离与外来记录不误杀）与可自动化 WIN 项在重建产物上重跑通过（安装版与便携版均 8 秒级就绪、跨形态单实例、数据目录共享、孤儿 Sidecar 卸载完整清理）。返验期间本机出现的 WebView2 用户数据损坏（启动就绪超时）已定位并处置，属环境性、非产品缺陷（记录于 `w7-windows-return.md`）。同 commit 的 CI run `35330773967` 已触发（结果见 handoff 记录）。
- **W7b 验收口径修复与完整验收**：解决"声称完整重跑、却又保留六项未运行"的口径冲突——先在 `31a3442` 产物上实际执行六项人工验收（WIN-02/03 真实会话创建、WIN-04 跨形态 UI 级会话/Provider 双向共享、WIN-09 免安装目录删除、WIN-11 最终构建菜单目视、WIN-13 代表性 v0.1.3 旧会话升级），再将 `windows-acceptance.mjs` 的机器状态由 `partially-accepted-on-device` 更新为 `accepted-on-device`（测试同步更新，历史枚举值保留）。因该文件参与构建输入 digest，状态变更后完整重建（`06bfdfaf…`），并在**新构建产物**上重跑 CLI 矩阵 T1–T11 与全部六项人工验收，`pnpm check` 全绿（Rust 49 / Unit 65 / Contract 9 / Integration 1 / Security 1）。最终资产即 §4 所列。W7b 的提交 `03fc6be` 已推送，同 commit 的 CI run `35488211340` 已成功完成。
- **最终 macOS C0 与 C1/C2 收口**：导入并校验 W7b 回传后，双平台输入 digest 均与目标一致；最终 macOS 构建、smoke、产物边界比较、体积门禁、DMG 校验、原生菜单/About 目视、Quit 与窗口关闭清理全部通过。C1 随后发现 3 个发布支持资产/文档阻塞项（report 的 license 状态过期、C0 口径过期、audit 口径过期）、1 个 provenance 缺口与 2 个整理项；C2 按依赖顺序重生成支持资产，在不改变源码 digest 的前提下增加不可变 Windows-local provenance 记录，同步公开四态文档，恢复生成 schema 噪声并重跑最终检查。当前没有 P0 或仍未关闭的 P1/P2 发布阻塞项。
